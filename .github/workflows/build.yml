name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Allows manual trigger from Actions tab

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    # ═══════════════════════════════════════════════════════════
    # CLEAR GRADLE CACHE - FORCES CLEAN BUILD
    # ═══════════════════════════════════════════════════════════
    - name: Clear Gradle cache
      run: |
        rm -rf ~/.gradle/caches
        rm -rf ~/.gradle/wrapper
        rm -rf .gradle
        rm -rf app/build

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: 8.2
        cache-disabled: true  # Disable Gradle cache

    - name: Generate Gradle Wrapper
      run: gradle wrapper --gradle-version 8.2

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # ═══════════════════════════════════════════════════════════
    # CLEAN BUILD - NO CACHE
    # ═══════════════════════════════════════════════════════════
    - name: Clean build
      run: ./gradlew clean --no-daemon --no-build-cache

    - name: Build Debug APK
      run: ./gradlew assembleDebug --stacktrace --no-daemon --no-build-cache

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: gesture-recognition-debug-${{ github.run_number }}
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30

    - name: Build Release APK (unsigned)
      run: ./gradlew assembleRelease --stacktrace --no-daemon --no-build-cache
      continue-on-error: true

    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: gesture-recognition-release-${{ github.run_number }}
        path: app/build/outputs/apk/release/app-release-unsigned.apk
        retention-days: 30